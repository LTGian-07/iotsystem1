runtime: php82
entrypoint: serve index.php

instance_class: F1

automatic_scaling:
  max_instances: 5

env_variables:
  MONGODB_URI: "mongodb+srv://legiangquangtan_db_user:admin123@cluster0.x2aiwzz.mongodb.net/iot_system?retryWrites=true&w=majority&appName=Cluster0"
  MONGODB_DB: "iot_system"

handlers:
- url: /(.*\.(css|js|png|jpg|ico|svg|json))$
  static_files: \1
  upload: .*\.(css|js|png|jpg|ico|svg|json)$

- url: /.*
  script: auto
